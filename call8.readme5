Despues de hacer git pull

En ~/code/call8>

cd ~/code/call8/config

sudo cp broadcasting.php.do broadcasting.php

cd ~/code/call8/resources/js

sudo cp bootstrap.js.do bootstrap.js

docker exec -it node sh

npm run prod

exit

cd ~/code/traefik

./dock restart
./dock art config:clear
./dock art storage:link

